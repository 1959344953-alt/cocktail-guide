# 调酒指南 · 部署与维护手册

「我们的身体不需要酒精，但灵魂需要！」

## 项目结构

```
cocktail-guide/
├── design/            # 设计原型（变体A 暗夜酒馆，本地双击可看）
└── web/               # Next.js 正式版（部署对象）
    ├── app/           # 页面
    │   ├── page.tsx          首页（hero + 热门配方 + 故事）
    │   ├── cocktails/        配方库 + /cocktails/[id] 每杯独立页（SEO 关键）
    │   ├── bar/              我的酒柜（三档匹配：能做/只差一个/缺工具）
    │   ├── taste/            按味道找
    │   ├── convenience/      便利店调酒
    │   ├── stories/          故事文化
    │   ├── publish/          发布配方（当前本地保存，待接数据库）
    │   ├── sitemap.ts        sitemap.xml（提交搜索引擎用）
    │   └── robots.ts
    └── lib/data.ts    # 全部配方/故事/基酒数据（加配方就改这里）
```

## 本地运行

```bash
cd web
npm install
npm run dev        # http://localhost:3000
npm run build      # 生产构建（部署前必跑，已验证通过）
npm start          # 生产模式运行
```

## 部署到 Vercel（免备案，海外服务器）

1. 把项目推到 GitHub（见下方「推送 GitHub」）
2. 打开 https://vercel.com ，用 GitHub 账号登录
3. 「Add New → Project」→ 选择 `cocktail-guide` 仓库
4. Framework Preset 自动识别为 Next.js，直接 Deploy
5. 等 1-2 分钟，得到 `cocktail-guide-xxxx.vercel.app` 地址
6. （可选）Settings → Domains 绑自己的域名

> 每次改完代码 push 到 GitHub，Vercel 自动重新部署。

## 推送 GitHub

```bash
cd C:\Users\sgc12\cocktail-guide
git add -A
git commit -m "改动说明"
git remote add origin https://github.com/1959344953-alt/cocktail-guide.git
git push -u origin main
```

（凭证已配置，直接 push 即可。若 GitHub 建仓需要手动：网页登录 github.com → New repository → 命名 cocktail-guide → 勾选 Private 或 Public → 创建。）

## 加新配方（重点！）

编辑 `web/lib/data.ts`，在 `COCKTAILS` 数组里加一个对象：

```ts
{
  id: "my-drink",           // 英文短横线，决定 URL /cocktails/my-drink
  name: "我的酒", en: "My Drink", cat: "原创", base: "金酒",
  level: "简单", abv: "15%", time: "3 分钟", glass: "古典杯",
  emoji: "🍸", color: "#C9A24B",
  photo: "https://images.unsplash.com/photo-XXXX?w=800&q=80",  // 真实照片
  tags: ["清爽", "适合新手"],
  tools: ["摇壶", "古典杯"],
  intro: "一句话简介。",
  ingredients: [{ name: "金酒", amt: "45ml" }, { name: "糖浆", amt: "10ml" }],
  steps: ["第一步", "第二步"],
  video: "https://www.bilibili.com/",
  story: "背后的故事。",
},
```

保存后 push，Vercel 自动上线，新配方立刻有自己的独立网址。

## 便利店配方（球头自己整理的）

同样加到 `web/lib/data.ts` 的 `CONVENIENCE` 数组，加 `conv: true` 标记即可。

## 提交搜索引擎（被搜到）

部署完成后（要等 HTTPS 生效）：

1. **Bing 站长工具**（推荐，国内可访问）：
   - 打开 https://www.bing.com/webmasters → 用 Microsoft 账号登录
   - 添加网站：`https://你的地址.vercel.app`
   - 验证方式选「DNS」或「HTML 文件」按提示操作
   - 提交 sitemap：`https://你的地址.vercel.app/sitemap.xml`
   - 一般 3-30 天开始收录

2. **Google Search Console**（如果不在乎墙内）：
   - https://search.google.com/search-console 同样流程

## 后续优化路线

- [ ] 发布功能接入 Supabase（已有账号），用户配方真存真展示
- [ ] PWA：manifest + 添加到主屏幕（手机像 App 一样用）
- [ ] 便利店配方批量整理上线（球头的抖音收藏）
- [ ] 每杯配方加「收藏」，localStorage 保存
- [ ] 详情页加「复制教程」按钮
- [ ] 首页「换一杯」随机推荐（客户端交互）
